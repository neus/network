# @neus/mcp-server

**Use the hosted endpoint:** `https://mcp.neus.network/mcp` — [setup](https://docs.neus.network/mcp/setup).

npm package **`@neus/mcp-server`**, Node ≥ 22.13 (`package.json`). Most integrations only need the URL above.

Sources and `server.json` live in this repo under `mcp/`. Guides: [docs.neus.network/mcp](https://docs.neus.network/mcp/overview).

Apache-2.0

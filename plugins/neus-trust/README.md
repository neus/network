# NEUS Trust

**Stop re-proving trust every session.**

Reusable identity receipts, scoped agent authority, and encrypted secrets at **`https://mcp.neus.network/mcp`**.

**[Install guide →](https://docs.neus.network/mcp/ide-plugin)**

```bash
npx -y -p @neus/sdk neus setup
npx -y -p @neus/sdk neus auth
```

Claude Code plugin:

```text
/plugin install neus-trust@neus
```

Skill: `/neus-trust:neus-trust-workflow`

---
title: Platform Setup
description: DNS, org verify, apps, sponsors.
icon: building
---

After [Get started](../get-started) and [Integration](../integration): **domains**, **org**, **apps**, **sponsors**. Receipts / privacy: [Proof receipts](./receipts-and-results) · [Security and trust](./security-and-trust)

[Hub](https://neus.network/hub) for profile, apps, credits.

## DNS domain

TXT at `_neus.<domain>`:

```text
neus=<your-wallet-address>
```

Then prove (example):

```javascript
const proof = await client.verify({
  verifier: 'ownership-dns-txt',
  data: { domain: 'yourdomain.com', walletAddress: '0x...' },
  walletAddress: '0x...',
  signature: '0x...',
  signedTimestamp: Date.now(),
});
```

Confirm:

```bash
GET /api/v1/proofs/check?verifierIds=ownership-dns-txt&address=0x...
```

## Organization

Google Workspace / Microsoft 365 → hosted verify; NEUS runs OAuth.

1. `https://neus.network/verify`
2. `preset=org-pack` when membership alone is enough
3. `returnUrl` or popup
4. Store receipt id

```text
https://neus.network/verify?preset=org-pack&returnUrl=https://app.example.com/verified
```

```text
https://neus.network/verify?preset=org-pack&mode=popup&origin=https://app.example.com
```

One company only:

```jsx
<VerifyGate
  hostedCheckoutUrl="https://neus.network/verify"
  requiredVerifiers={['ownership-org-oauth']}
  verifierData={{
    'ownership-org-oauth': { provider: 'google', expectedOrgDomain: 'acme.com' },
  }}
>
  <ProtectedContent />
</VerifyGate>
```

You rarely need your own Google/Microsoft OAuth app.

## Link app

|  |  |
|--|--|
| 1 | Hub → link app |
| 2 | App domain |
| 3 | Daily credit cap |
| 4 | Lock **Origin** in prod |
| 5 | `X-Neus-App` + `Origin` from allowed origins |

## Attribution

`appId` is public—not secret. Send `X-Neus-App: <appId>`.

```javascript
const client = new NeusClient({ appId: 'your-app-id' });
```

<Card title="Billing" icon="credit-card" href="./billing">
  Credits and sponsors.
</Card>

## Sponsor grants

```javascript
new NeusClient({ appId: 'your-app-id', sponsorGrant: '<token>' });
```

<Warning>
Short-lived tokens. Re-issue per session. No long-lived secrets in client bundles.
</Warning>

Hosted UX: [Hosted Verify](../cookbook/auth-hosted-verify) · [Integration](../integration)

## Related

<CardGroup cols={2}>
  <Card title="Get started" icon="book" href="../get-started">
    Onboarding.
  </Card>
  <Card title="Proof receipts" icon="receipt" href="./receipts-and-results">
    Save ids.
  </Card>
  <Card title="Security" icon="lock" href="./security-and-trust">
    Visibility.
  </Card>
  <Card title="Billing" icon="credit-card" href="./billing">
    Payers.
  </Card>
</CardGroup>

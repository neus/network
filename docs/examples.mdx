---
title: Examples
description: Runnable Node, React, and curl samples.
icon: code
---

**check → verify → save id → reuse.** Production: [Get started](./get-started) · [Billing](./platform/billing)

**Prefer creating proofs** via [Quickstart](./quickstart), [Hosted Verify](./cookbook/auth-hosted-verify), or **widgets**—not raw HTTP.

| Repository | Contents |
|------------|----------|
| [react-component](https://github.com/neus/network/tree/main/examples/react-component) | Vite, VerifyGate, browser wallet |
| [nodejs-basic](https://github.com/neus/network/tree/main/examples/nodejs-basic) | **Advanced:** Node, server-held key, standardize + sign + `gateCheck` |
| [curl-minimal](https://github.com/neus/network/tree/main/examples/curl-minimal) | **Advanced:** HTTP-only standardize → sign → submit |

## React: gate

```jsx
import { VerifyGate } from '@neus/sdk/widgets';

export default function ProtectedPage() {
  return (
    <VerifyGate
      appId="your-app-id"
      requiredVerifiers={['nft-ownership']}
      verifierData={{
        'nft-ownership': {
          contractAddress: '0x...',
          tokenId: '1',
          chainId: 8453,
        },
      }}
    >
      <ProtectedContent />
    </VerifyGate>
  );
}
```

## Node: proof

For **server-held keys** or **HTTP-only** stacks: **standardize → sign → submit** ([JavaScript SDK](./sdks/javascript)) or [nodejs-basic](https://github.com/neus/network/tree/main/examples/nodejs-basic). For most products, use **Hosted Verify** or **browser `client.verify()`** instead so you are not maintaining signing plumbing.

## HTTP (advanced)

```bash
curl -X POST https://api.neus.network/api/v1/verification/standardize \
  -H "Content-Type: application/json" \
  -d '{"walletAddress":"0x...","verifierIds":["ownership-basic"],"data":{"content":"Hello NEUS"}}'
# sign signerString, then POST /api/v1/verification with the same body + signature
```

## Docs

|  |  |
|--|--|
| [Get started](./get-started) | App + billing |
| [Hosted Verify](./cookbook/auth-hosted-verify) | Hosted UX |
| [MCP](./mcp/overview) | Assistants |
| [Quickstart](./quickstart) | First SDK proof |
| [API](./api/overview) | HTTP |

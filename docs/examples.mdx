---
title: Examples
description: Trust receipts showcase, Node, React, and HTTP reference implementations.
icon: code
---

Same flow everywhere: check → verify if needed → save **`qHash`** → reuse. Register your app and set billing: [Get started](./get-started).

**Showcase:** [trust-receipts-showcase](https://github.com/neus/network/tree/main/examples/trust-receipts-showcase) · **Demo:** [neus.network/demo](https://neus.network/demo).

[Quickstart](./quickstart) · [Hosted Verify](./cookbook/auth-hosted-verify). Server-only integration: [Integrations](./cookbook/integrations).

| Example | Use it for |
|------------|----------|
| [**trust-receipts-showcase** (default)](https://github.com/neus/network/tree/main/examples/trust-receipts-showcase) | Full **VerifyGate** + gate check: each card is a trust outcome and a verifier |
| [nodejs-basic](https://github.com/neus/network/tree/main/examples/nodejs-basic) | Server key, sign + `gateCheck` |

Raw HTTP signing examples are inline below.

## React: gate

```jsx
import { VerifyGate } from '@neus/sdk/widgets';

export default function ProtectedPage() {
  return (
    <VerifyGate
      appId="your-app-id"
      billingWallet="0xYourHubWallet"
      requiredVerifiers={['ownership-basic']}
    >
      <ProtectedContent />
    </VerifyGate>
  );
}
```

## Node

[Integrations](./cookbook/integrations) for backend receipt creation. [nodejs-basic](https://github.com/neus/network/tree/main/examples/nodejs-basic) for lower-level SDK usage.

## HTTP reference

```bash
curl -X POST https://api.neus.network/api/v1/verification/standardize \
  -H "Content-Type: application/json" \
  -d '{"walletAddress":"0x...","verifierIds":["ownership-basic"],"data":{"content":"Hello NEUS","owner":"0x..."},"signedTimestamp":1713312000000}'
# Then sign signerString and POST /api/v1/verification with the same body + signature
```

## Doc map

| Doc | Purpose |
|-----|---------|
| [Get started](./get-started) | Register app and billing |
| [Hosted Verify](./cookbook/auth-hosted-verify) | Browser ceremony on NEUS |
| [MCP](./mcp/overview) | Assistants and automation |
| [Quickstart](./quickstart) | SDK first receipt |
| [API](./api/overview) | HTTP reference |

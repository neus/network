---
title: Get started
description: Account, app, first receipt.
icon: book
---

## Docs path (pick one lane)

| Lane | Start here | Then |
|------|------------|------|
| **Ship checks in your app** | This page (account + `appId`) | [Quickstart](./quickstart) → [Integration](./integration) → [Hosted Verify](./cookbook/auth-hosted-verify) when you need redirect/popup detail |
| **Wire editors / assistants (MCP)** | [MCP setup](./mcp/setup) | [MCP overview](./mcp/overview) → [Agent verification flow](./agents/agent-verification-flow) when you use an **agent wallet** |
| **Hub / DNS / org settings** | [Platform setup](./platform/setup) | [Billing](./platform/billing) as needed |

Avoid parallel “first setup” threads: use **Quickstart** for SDK code, **MCP setup** for hosted MCP install only, and **Agent verification flow** for `neus_agent_link` / `neus_agent_create` together.

## 1. NEUS account

[Hub](https://neus.network/hub): sign in to NEUS (passkey or wallet on **NEUS** only).

## 2. App in Platform

Create the app, copy **`appId`**. Usage bills the linked profile.

## 3. First receipt

[Quickstart](./quickstart): **browser** (`VerifyGate` or hosted URL → `/verify`) and **server** (`verifyFromApp`) on one page.

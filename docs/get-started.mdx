---
title: Get started
description: Builder onboarding: account, app, profile, integration loop, and production readiness.
icon: book
---

One **proof receipt ID** (`proofId`, same value as `qHash` on the wire) feeds gates, checks, and shareable proof pages. **[Proof receipts](./platform/receipts-and-results)** defines what to save. **[Security and trust](./platform/security-and-trust)** defines visibility.

## 1. Create your account

1. Sign in at [`https://neus.network/hub`](https://neus.network/hub) with passkey or wallet.
2. Complete profile basics so you can see your **Apps**, **Credits**, and **Access keys**.

## 2. Run your first loop locally

Use **[Quickstart](./quickstart)** for a direct `ownership-basic` proof with the SDK, or **[Hosted Verify](./cookbook/auth-hosted-verify)** when the flow is interactive (social, org, human). **[Integration](./integration)** describes check, verify, save, reuse for every path.

## 3. Pick how you integrate

**[Choose an integration path](./choose-an-integration-path)** matches outcomes to surfaces: hosted verify, SDK, VerifyGate, `gateCheck`, MCP.

## 4. Platform setup for production domains

When you need DNS domain proof, org OAuth, app linking, or sponsor grants at scale, follow **[Platform setup](./platform/setup)** after you understand the integration loop.

## Before production

Three decisions: **create an app**, **choose who pays**, and **generate an access key** only when you need account-scoped API or MCP tools. Details for headers and limits live in **[Billing](./platform/billing)** and **[Platform setup](./platform/setup)**.

### Create an app

1. Open **[Apps](https://neus.network/profile?tab=apps)** in your NEUS account.
2. **Link app** or **Manage app**.
3. Enter your **app domain** (for example `acme.com`), set a **daily credit cap**, and finish the modal.

When registration succeeds, NEUS issues an **`appId`**. The SDK sends it as **`X-Neus-App`** on API traffic.

```javascript
import { NeusClient } from '@neus/sdk';

const client = new NeusClient({
  appId: 'your-app-id', // same value sent as X-Neus-App
});
```

<Tip>
  **`appId`** tells NEUS **which product** made the request. It does **not** by itself move charges to your organization. When **you** pay for users, add **sponsor grants** and org wallet credits. See **Choose who pays** and **[Billing](./platform/billing)**.
</Tip>

### Choose who pays

1. **Your product pays:** fund credits on the **organization wallet** tied to your app, issue short-lived **sponsor grants**, pass **`sponsorGrant`** on the SDK (maps to **`X-Sponsor-Grant`**). **[Platform setup](./platform/setup)** has the full flow.
2. **Users pay:** they use **their** credits (or **x402** when applicable). Do **not** send sponsor grants.

```javascript
const client = new NeusClient({
  appId: 'your-app-id',
  sponsorGrant: '<sponsor-grant-token>',
});
```

### Generate an access key for account-scoped tools

Use **[Access keys](https://neus.network/profile?tab=account)** when you need **`Authorization: Bearer`** on **`api.neus.network`**, scripts, or MCP tools such as **`neus_me`** and private reads. You can skip this until something returns **`auth_required`**. **[MCP Auth](./mcp/auth)**

Do **not** ship this key in browser bundles. End users complete interactive steps on **Hosted Verify**, not with your personal access key.

### Production readiness checklist

- [ ] App registered with a realistic daily cap
- [ ] Paying wallet has credits (yours or users', based on your model)
- [ ] Client sends **`appId`** and uses **`sponsorGrant`** only when you sponsor
- [ ] Team understands **[Billing](./platform/billing)** and **[x402](./platform/x402)**
- [ ] Verification receipts are stored and reused (see **[Proof receipts](./platform/receipts-and-results)**)

## Related

<CardGroup cols={2}>
  <Card title="Billing" icon="credit-card" href="./platform/billing">
    Who pays, x402, sponsor model.
  </Card>
  <Card title="Platform setup" icon="building" href="./platform/setup">
    Domains, org verify, app linking, sponsors.
  </Card>
  <Card title="Integration" icon="compass" href="./integration">
    Verify, save, reuse loop.
  </Card>
  <Card title="Security and trust" icon="lock" href="./platform/security-and-trust">
    Visibility modes.
  </Card>
</CardGroup>

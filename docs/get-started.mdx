---
title: Get started
description: Account, app, billing, and your first proof loop.
icon: book
---

<Note>
**Default:** [Hosted Verify](./cookbook/auth-hosted-verify) for users; you store the **receipt id** and **check** before asking them to verify again ([Integration](./integration#loop)). **Shipped products:** plan for **org-paid credits + sponsor grants** unless you intentionally use self-serve billing ([Choose who pays](#choose-who-pays)).
</Note>

One receipt id powers gates, checks, and proof pages. [Proof receipts](./platform/receipts-and-results) · [Security and trust](./platform/security-and-trust)

| Topic | Reference |
|-------|-----------|
| Check before re-verifying | [Integration](./integration#loop) |
| Org-sponsored usage | [Choose who pays](#choose-who-pays) |
| Sample repositories | [Examples](./examples) |

## 1. Account

1. [Hub](https://neus.network/hub)—passkey or wallet.
2. Finish profile so **Apps**, **Credits**, and **Access keys** are available.

## 2. First loop

Browser-friendly verification → [Hosted Verify](./cookbook/auth-hosted-verify). First SDK wallet sign → [Quickstart](./quickstart). Loop → [Integration](./integration).

## 3. Production setup

DNS, org OAuth, apps, sponsors at scale → [Platform setup](./platform/setup) after the loop is clear.

## Ship to production

Decisions: **app**, **who pays**, **access key** (only for account-scoped API/MCP). Details: [Billing](./platform/billing) · [Platform setup](./platform/setup)

### Create an app

1. [Apps](https://neus.network/profile?tab=apps) → **Link app** / **Manage app**.
2. **App domain** + **daily credit cap**.

You get an `appId`; pass it into the SDK so NEUS can attribute usage to your app.

```javascript
import { NeusClient } from '@neus/sdk';

const client = new NeusClient({
  appId: 'your-app-id',
});
```

`appId` is attribution, not billing. To pay for users: sponsor grants + org credits. [Billing](./platform/billing)

### Choose who pays

**Shipped products:** org credits plus a **sponsor grant** on SDK calls so usage bills your org. Users shouldn’t hit “buy credits” first. [Platform setup](./platform/setup)

**Self-serve:** user credits or x402—no sponsor on those flows.

```javascript
const client = new NeusClient({
  appId: 'your-app-id',
  sponsorGrant: '<sponsor-grant-token>',
});
```

### Access keys

[Access keys](https://neus.network/profile?tab=account) when a tool or script needs to act on **your** account (for example certain MCP tools). Create a key only after the product returns **`auth_required`**. [MCP Auth](./mcp/auth)

Never ship this key in the browser. Users use Hosted Verify, not your personal key.

### Checklist

- [ ] App + realistic daily cap
- [ ] Credits for your payer model
- [ ] `appId` always; `sponsorGrant` only when you sponsor
- [ ] Team read [Billing](./platform/billing) and [x402](./platform/x402)
- [ ] Receipt ids stored and reused ([Proof receipts](./platform/receipts-and-results))

## Related

<CardGroup cols={2}>
  <Card title="Agents (portable identity)" icon="robot" href="./agents/overview">
    Proof-backed profile, shareable card URL, MCP lookup—chat UIs and IDEs.
  </Card>
  <Card title="Billing" icon="credit-card" href="./platform/billing">
    Who pays, x402, sponsors.
  </Card>
  <Card title="Platform setup" icon="building" href="./platform/setup">
    Domains, org, apps, sponsors.
  </Card>
  <Card title="Integration" icon="compass" href="./integration">
    Production loop.
  </Card>
  <Card title="Security and trust" icon="lock" href="./platform/security-and-trust">
    Who can see proofs and receipts.
  </Card>
</CardGroup>

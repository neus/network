---
title: Production Integration
description: From first proof to shipped verification.
icon: compass
---

<Note>
**Most teams start here:** [Hosted Verify](./cookbook/auth-hosted-verify) → persist **proof receipt ID** → run checks before prompting verify again → **app pays** by default ([Get started](./get-started#choose-who-pays)).
</Note>

Surfaces: [Hosted Verify](./cookbook/auth-hosted-verify), [SDK](./sdks/overview), [Widgets](./widgets/overview), [API](./api/overview), [MCP](./mcp/overview). [Billing](./platform/billing). [LLM docs](./platform/llm-docs).

Call `GET /api/v1/proofs/check` (or use a stored `proofId`) before prompting verify. Persist **`proofId`** (same as **`qHash`**) — [Proof receipts](./platform/receipts-and-results).

## Loop

1. **Check:** eligibility with a saved `proofId` or **`GET /api/v1/proofs/check`** before showing a verify flow
2. **Verify:** only when needed (**[Hosted Verify](./cookbook/auth-hosted-verify)** or SDK/API)
3. **Save:** `proofId` on the user or session (same value as **`qHash`**; see **[Proof receipts](./platform/receipts-and-results)**)
4. **Reuse:** UI, `VerifyGate`, server policy, MCP. Same ID everywhere

## Visibility modes

Choose visibility once per proof. Full detail and `storeOriginalContent` guidance are in **[Security and trust](./platform/security-and-trust)**.

| Mode | `privacyLevel` | `publicDisplay` | Use |
|------|----------------|-----------------|-----|
| Listed public | `public` | `true` | Publishing and attribution where the proof should be discoverable |
| Unlisted public | `public` | `false` | Non-sensitive reuse, gates, and checks without listing for discovery |
| Owner-only | `private` | `false` | Owner-only access and owner-authenticated reuse |

- **Raw SDK `client.verify()`** defaults to **private** stored receipts (see **[SDK overview](./sdks/overview)**).
- **`VerifyGate`** create mode defaults to **unlisted public** (`public` + `publicDisplay: false`) so the primary gating surface matches reuse-first flows.

## Checklist

<Checklist>
  <Item>Pick Hosted Verify, Widgets, SDK, API, or MCP (one primary path)</Item>
  <Item>Gate before prompting for verification</Item>
  <Item>Persist proof receipt IDs, not raw signatures</Item>
  <Item>Test: verify → save → check → reuse</Item>
  <Item>[Get started](./get-started) · [Billing](./platform/billing)</Item>
</Checklist>

## Verification strategies (SDK / API)

| Strategy | When |
|----------|------|
| `reuse-or-create` | Default: reuse a valid proof if you have one; otherwise start verification |
| `fresh` | Force a new verification even when an older proof might still be valid |
| `reuse` | Never create; require an existing proof or fail (read-only / display paths) |

## URLs

| Purpose | URL |
|---------|-----|
| Hosted Verify | `https://neus.network/verify` |
| API | `https://api.neus.network` |
| MCP | `https://mcp.neus.network/mcp` |

## Related

<CardGroup cols={2}>
  <Card title="Get started" icon="book" href="./get-started">
    Onboarding and production checklist.
  </Card>
  <Card title="Hosted Verify" icon="key" href="./cookbook/auth-hosted-verify">
    Hosted flows.
  </Card>
  <Card title="Receipts" icon="receipt" href="./platform/receipts-and-results">
    Persist IDs.
  </Card>
  <Card title="Security and trust" icon="lock" href="./platform/security-and-trust">
    Visibility.
  </Card>
  <Card title="API" icon="terminal" href="./api/overview">
    HTTP.
  </Card>
  <Card title="Verifiers" icon="list-tree" href="./verification/verifiers">
    Full list.
  </Card>
  <Card title="Billing" icon="credit-card" href="./platform/billing">
    Payers, sponsors, x402.
  </Card>
</CardGroup>

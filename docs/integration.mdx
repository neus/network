---
title: Integration
description: Check, verify, save, reuse — one loop for apps, gates, profiles, and agents.
icon: compass
---

Every NEUS integration follows the same loop.

The same trust receipt works in a React widget, a server gate, an MCP assistant, or a partner app — because the **receipt** is the trust context, not a session flag in your database.

**Profile → portal → check → receipt:** builders publish access from their NEUS profile; visitors complete hosted sign-in or trust checks on NEUS; receipts attach to profiles and reuse everywhere. See [Trust stack](./platform/trust-stack) for how sign-in, gates, and agents fit together.

| Step | Do this                                                                       |
| ---- | ----------------------------------------------------------------------------- |
| 1    | Check whether you already have a valid receipt                                |
| 2    | Verify only when the receipt is missing, stale, or needs a new trust decision |
| 3    | Save the trust receipt on your user, account, agent, or record (API field: `qHash`) |
| 4    | Reuse the receipt before access, payment, delegation, or action               |

## Where code runs

| Layer     | Use                                                                       |
| --------- | ------------------------------------------------------------------------- |
| Browser   | `VerifyGate`, `getHostedCheckoutUrl`, hosted `/verify`                    |
| Server    | `gateCheck` with `gateId`                                                 |
| Assistant | NEUS MCP — reuse receipts before action ([Install](./install)) |

Passkey, wallet, and OAuth flows should run on NEUS hosted pages by default. Do not ship access keys or server secrets in browser bundles.

Backend receipt creation and access keys: [Integrations](./cookbook/integrations).

## Integration options

<CardGroup cols={2}>
  <Card
    title="Hosted Verify"
    icon="shield-check"
    href="./cookbook/auth-hosted-verify"
  >
    Browser flow owned by NEUS.
  </Card>
  <Card title="SDK" icon="code" href="./sdks/javascript">
    JavaScript helpers for app and server flows.
  </Card>
  <Card title="VerifyGate" icon="square-check" href="./widgets/verifygate">
    React access gates.
  </Card>
  <Card title="Agents" icon="robot" href="./agents/agent-verification-flow">
    Agent identity and delegation.
  </Card>
</CardGroup>

Receipt fields and URLs: [Receipts and results](./platform/receipts-and-results). Advanced identity encoding: [CAIP-380](./learn/standards/caip-380).

**Next:** [Hosted Verify](./cookbook/auth-hosted-verify)

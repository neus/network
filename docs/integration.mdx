---
title: Production Integration
description: Check, verify, save, reuse—ship path.
icon: compass
---

[Hosted Verify](./cookbook/auth-hosted-verify) · [SDK](./sdks/overview) · [Widgets](./widgets/overview) · [API](./api/overview) · [MCP](./mcp/overview) · [Billing](./platform/billing) · [LLM docs](./platform/llm-docs)

**Check** (`GET /api/v1/proofs/check` or a saved receipt id) before prompting verify again. **Save** the receipt id from the response ([Proof receipts](./platform/receipts-and-results)).

## Loop

1. **Check** — saved id or proofs/check
2. **Verify** — only if needed (hosted, SDK, or API)
3. **Save** — receipt id on user/session
4. **Reuse** — UI, VerifyGate, server, MCP

| Situation | Approach |
|-----------|----------|
| Repeat visits | Run `gateCheck` or use a stored receipt id before a new verify |
| Balance / NFT gates | Narrow `since` or `fresh` strategy — [SDK verifications](./sdks/verifications) |
| Org and domain | [Platform setup](./platform/setup), then the same loop |

## Visibility

[Security and trust](./platform/security-and-trust) has the full matrix.

| Mode | Use |
|------|-----|
| Listed public | Discoverable attribution |
| Unlisted public | Gates and reuse, not listed |
| Owner-only | Signed-in owner |

SDK `client.verify()` defaults **private**. VerifyGate create defaults **unlisted public**.

## Checklist

<Checklist>
  <Item>One primary path: Hosted Verify, Widgets, SDK, API, or MCP</Item>
  <Item>Check before showing verify</Item>
  <Item>Store receipt ids—not only raw signatures</Item>
  <Item>Test: verify → save → check → reuse</Item>
  <Item>[Get started](./get-started) · [Billing](./platform/billing)</Item>
</Checklist>

## Strategies (SDK / API)

| Strategy | When |
|----------|------|
| `reuse-or-create` | Default: reuse if valid, else verify |
| `fresh` | Always new proof |
| `reuse` | Existing proof only |

## URLs

|  |  |
|--|--|
| Hosted Verify | `https://neus.network/verify` |
| API | `https://api.neus.network` |
| MCP | `https://mcp.neus.network/mcp` |

## Related

<CardGroup cols={2}>
  <Card title="Get started" icon="book" href="./get-started">
    Onboarding.
  </Card>
  <Card title="Hosted Verify" icon="key" href="./cookbook/auth-hosted-verify">
    Hosted UX.
  </Card>
  <Card title="Receipts" icon="receipt" href="./platform/receipts-and-results">
    What to store.
  </Card>
  <Card title="Security and trust" icon="lock" href="./platform/security-and-trust">
    Visibility.
  </Card>
  <Card title="API" icon="terminal" href="./api/overview">
    HTTP.
  </Card>
  <Card title="Verifiers" icon="list-tree" href="./verification/verifiers">
    Catalog.
  </Card>
  <Card title="Billing" icon="credit-card" href="./platform/billing">
    Payers, x402.
  </Card>
</CardGroup>

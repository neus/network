{
  "type": "object",
  "required": [
    "contractAddress",
    "tokenId",
    "chainId"
  ],
  "properties": {
    "ownerAddress": {
      "type": "string",
      "pattern": "^0x[a-fA-F0-9]{40}$",
      "description": "NFT owner address (optional; defaults to request walletAddress unless sponsored mode delegates)"
    },
    "contractAddress": {
      "type": "string",
      "pattern": "^0x[a-fA-F0-9]{40}$",
      "description": "NFT contract address"
    },
    "tokenId": {
      "type": "string",
      "description": "NFT token ID"
    },
    "tokenType": {
      "type": "string",
      "enum": ["erc721", "erc1155"],
      "description": "Token standard (default: erc721)"
    },
    "chainId": {
      "type": "integer",
      "description": "Chain ID where the NFT contract is deployed (testnet or mainnet). The backend reads from this chain using server-side RPC configuration."
    },
    "blockNumber": {
      "type": "integer",
      "description": "Optional block number for a point-in-time ownership check"
    }
  }
}
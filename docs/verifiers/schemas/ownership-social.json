{
  "$id": "https://neus.network/schemas/ownership-social.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Social media account ownership via OAuth",
  "properties": {
    "internalSocialToken": {
      "description": "Opaque token from the hosted link session after the user authorizes. Populated by the hosted flow; do not supply in API calls.",
      "type": "string"
    },
    "provider": {
      "description": "OAuth provider to verify against.",
      "enum": [
        "discord",
        "github",
        "facebook",
        "x",
        "farcaster",
        "linkedin",
        "telegram",
        "coinbase"
      ],
      "type": "string"
    },
    "walletAddress": {
      "description": "Wallet bound to the social account when applicable.",
      "format": "universal-address",
      "type": "string"
    }
  },
  "required": [
    "provider",
    "internalSocialToken"
  ],
  "title": "ownership-social",
  "type": "object"
}

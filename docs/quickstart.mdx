---
title: Quickstart (SDK)
description: First ownership-basic verification with the JavaScript SDK.
icon: rocket
---

Wallet **`ownership-basic`**. Social / human / org: [Hosted Verify](./cookbook/auth-hosted-verify). Production: [Get started](./get-started).

## 1. Install

```bash
npm install @neus/sdk
```

## 2. Create a proof

`client.verify()` defaults **private**. VerifyGate defaults differ - [Widgets](./widgets/overview), [Security and trust](./platform/security-and-trust).

```javascript
import { NeusClient } from '@neus/sdk';

const client = new NeusClient({
  appId: 'your-app-id', // optional locally; required in prod - [Get started](./get-started)
});

const proof = await client.verify({
  verifier: 'ownership-basic',
  content: 'My content or claim',
  wallet: window.ethereum,
});

const proofId = proof.proofId;
```

Save **`proofId`**. Same value is **`qHash`** in some API paths - [Verification results](./platform/receipts-and-results). **Check** before asking the user to verify again.

Other verifiers: [SDK verifications](./sdks/verifications). No in-page wallet: [Hosted Verify](./cookbook/auth-hosted-verify).

## 3. Poll

```javascript
const status = await client.pollProofStatus(proofId, {
  interval: 3000,
  timeout: 60000,
});
```

## 4. Gate (React)

```jsx
import { VerifyGate } from '@neus/sdk/widgets';

<VerifyGate
  appId="your-app-id"
  requiredVerifiers={['ownership-basic']}
>
  <ProtectedContent />
</VerifyGate>
```

[VerifyGate](./widgets/verifygate) | [Integration](./integration)

## 5. Server check

- **`gateCheck` `address`** = wallet that signed `verify()`.
- Prefer a **stored id** + check before a new verify.
- Verifier list must match what you issued.

```javascript
const result = await client.gateCheck({
  address: '0x...',
  verifierIds: ['ownership-basic'],
});
```

## See also

| | |
|--|--|
| [Get started](./get-started) | App, billing, production |
| [Choose a path](./choose-an-integration-path) | SDK, widgets, API, MCP |
| [Integration](./integration) | Check, verify, save, reuse |
| [Examples](./examples) | Repositories |

---
title: Agents Overview
description: Identity, authority, and receipts for agents that act.
icon: robot
---

NEUS gives agents identity, scoped authority, and receipts for trusted actions.

| Need | How |
|------|-----|
| Identify the agent | `agent-identity` |
| Set what it can do | `agent-delegation` |
| Check if ready | `neus_agent_link` |
| Reuse trust | Receipt ID, proof URL, or DID |
| Audit later | Receipts and proof reads |

## Flow

<Steps>
  <Step title="Import (optional)">
    ```bash
    npx -y -p @neus/sdk neus import
    ```
  </Step>
  <Step title="Connect">
    ```bash
    npx -y -p @neus/sdk neus setup
    npx -y -p @neus/sdk neus auth
    ```
    Start each session with **`neus_context`**.
  </Step>
  <Step title="Link">
    Call **`neus_agent_link`**. If **`linked: true`**, reuse existing receipts.
  </Step>
  <Step title="Create">
    Call **`neus_agent_create`** when proofs are missing. Finish on NEUS.
  </Step>
  <Step title="Act">
    Check receipts before payments, data access, or other trusted actions.
  </Step>
</Steps>

```json
{ "agentId": "my-agent", "agentWallet": "0x..." }
```

## Next

<CardGroup cols={2}>
  <Card title="Verification flow" icon="route" href="./agent-verification-flow">
    Step-by-step order.
  </Card>
  <Card title="Agent identity" icon="badge-check" href="./agent-identity">
    Register an agent.
  </Card>
  <Card title="Agent delegation" icon="key" href="./agent-delegation">
    Grant permissions.
  </Card>
  <Card title="MCP setup" icon="plug" href="../mcp/setup">
    Connect your assistant.
  </Card>
  <Card title="A2A discovery" icon="cards" href="./a2a">
    Agent cards.
  </Card>
</CardGroup>

SDK details: [Agent identity](./agent-identity) · [Agent delegation](./agent-delegation)

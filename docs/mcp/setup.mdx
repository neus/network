---
title: Setup
description: Quick setup for the hosted NEUS MCP endpoint.
icon: code
---

**Building on NEUS?** Do **[Get started](../get-started)** first so platform setup, **`appId`**, and credits match how you use MCP in production.

## Connect

Add this configuration to your MCP settings (Cursor, Claude Desktop, VS Code, or any client that supports HTTP MCP). Use the URL exactly as shown, including `/mcp`.

```json
{
  "mcpServers": {
    "neus": {
      "type": "streamableHttp",
      "url": "https://mcp.neus.network/mcp",
      "description": "NEUS MCP"
    }
  }
}
```

Reload the client if prompted. **No API key is required.**

**Self-hosted only:** `npx @neus/mcp-server` — most people use the hosted URL above.

## First run

1. Ask the assistant to run **`neus_verify_or_guide`** (or **`neus_proofs_check`**).
2. **Open** any **`https://neus.network/verify`** link in the browser.
3. Optional: **`neus_proofs_get`** when you have an id—see [Tools](./tools).
4. Sessions / private data: [Auth](./auth).

Then follow the [Overview](./overview) default path.

## Related

<CardGroup cols={2}>
  <Card title="Overview" icon="plug" href="./overview">
    What to do after connecting.
  </Card>
  <Card title="Tools" icon="wrench" href="./tools">
    Tool parameters.
  </Card>
  <Card title="Auth" icon="lock" href="./auth">
    Bearer and privacy.
  </Card>
  <Card title="Endpoints" icon="globe" href="./endpoints">
    Discovery and well-known URLs.
  </Card>
</CardGroup>

---
title: Setup
description: "Connect any MCP client to NEUS: identity, permissions, proofs, and Vault."
icon: code
---

**Hosted endpoint:** `https://mcp.neus.network/mcp`

## 1. Register NEUS

```bash
npx -y -p @neus/sdk neus setup
```

This installs the public workflow skill and configures supported MCP clients. It does not open a browser or sign you in.

To configure one client:

```bash
npx -y -p @neus/sdk neus setup --client cursor
npx -y -p @neus/sdk neus setup --client codex
npx -y -p @neus/sdk neus setup --client vscode
npx -y -p @neus/sdk neus setup --client claude
```

## 2. Sign in

Click **Connect** in your MCP client, or run:

```bash
npx -y -p @neus/sdk neus auth
```

Codex manages its own OAuth session:

```bash
npx -y -p @neus/sdk neus auth --client codex
```

## 3. Check the connection

```bash
npx -y -p @neus/sdk neus doctor --live
```

A healthy result confirms that the endpoint is registered, authentication works, and the public MCP tools are available.

## Or paste this into your agent

Copy the block below into Claude Code, Cursor, Codex, or any coding agent with NEUS MCP. The agent reads it, runs the three steps above, and starts carrying your authority.

```text
# NEUS connect in one prompt

You are connecting this editor to NEUS so my identity, provenance, and
authority travel with me. Follow these steps exactly.

## 1. Register NEUS
Run: npx -y -p @neus/sdk neus setup
This installs the public workflow skill and writes MCP config for every
supported host in this project. It does not open a browser.

## 2. Sign in
Run: npx -y -p @neus/sdk neus auth
Or click Connect in the MCP client. Default sign-in is browser OAuth.
No wallet required.

## 3. Confirm the connection
Run: npx -y -p @neus/sdk neus doctor --live
A healthy result confirms the endpoint, auth, and public tools.

## 4. What to do next
After setup, reuse my existing proofs before asking me to verify again.
Before any sensitive action (spend, publish, secrets, agent tool call),
call neus_context once, then neus_proofs_check. Summarize as
NEUS Verify: Passed, Action needed, or Blocked.

Canonical endpoint: https://mcp.neus.network/mcp
Docs: https://docs.neus.network/mcp/setup
```

## Client details

<Tabs sync={false}>
  <Tab title="Cursor">

Install `neus-mcp` from the NEUS marketplace, or use the CLI command above. The plugin registers the endpoint and bundles the public skills. When the plugin is installed, the CLI skips duplicate registration.

```text
Cursor → Settings → Cursor Plugins → Add marketplace
Marketplace URL: https://github.com/neus/network
Install: neus-mcp
```

  </Tab>
  <Tab title="Codex">

The CLI writes the MCP registration to `~/.codex/config.toml`. Install the optional skill bundle with:

```text
codex plugin marketplace add https://github.com/neus/network
codex plugin install neus-mcp
```

  </Tab>
  <Tab title="VS Code">

| OS | User config |
| --- | --- |
| macOS | `~/Library/Application Support/Code/User/mcp.json` |
| Linux | `~/.config/Code/User/mcp.json` |
| Windows | `%APPDATA%\Code\User/mcp.json` |

Project config: `./.vscode/mcp.json`

  </Tab>
  <Tab title="Claude Code">

The CLI registers NEUS in `~/.claude.json`. Install the optional plugin with:

```text
/plugin marketplace add https://github.com/neus/network
/plugin install neus-mcp@neus
/neus-mcp:neus-setup
```

  </Tab>
  <Tab title="Another MCP client">

Add one remote server:

```json
{
  "mcpServers": {
    "neus": {
      "url": "https://mcp.neus.network/mcp"
    }
  }
}
```

Your client discovers NEUS OAuth metadata from the hosted endpoint.

  </Tab>
</Tabs>

## Connect an agent to a project

After creating a Trusted Agent on your profile, load its current identity and permissions:

```bash
npx -y -p @neus/sdk neus mount <agentId> --apply cursor
```

Replace `cursor` with `claude` or `codex` for those hosts. The command writes `.neus/mount.json` and the selected host adapter. See [Connect agent context](../agents/runtime-mount).

## Access keys

Use OAuth for interactive clients. Use a [profile access key](https://neus.network/profile?tab=account) only when browser OAuth is unavailable, such as a server or CI job.

```bash
export NEUS_ACCESS_KEY=npk_...
npx -y -p @neus/sdk neus setup
npx -y -p @neus/sdk neus doctor --live
```

Treat the key like a password. Never paste it into chat or commit it to a repository.

## Disconnect

```bash
neus disconnect --access-key <token>
```

## Troubleshooting

- **No tools appear:** restart the MCP client after setup, then run `neus doctor --live`.
- **Authentication required:** click **Connect** or run `neus auth`.
- **Agent permission missing:** create or refresh the agent permission on NEUS, then run `neus mount` again.
- **Search-only endpoint:** use `https://mcp.neus.network/mcp`. `https://docs.neus.network/mcp` only searches documentation.

Next: [Run your first guarded action](./guarded-action).
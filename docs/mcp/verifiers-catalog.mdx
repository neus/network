---
title: Verifier Catalog (`neus_verifiers_catalog`)
description: Live verifier IDs and metadata from NEUS.
icon: table
---

Call before hardcoding **`verifierIds`** / policy — list is always current.

## Input

```json
{}
```

## Output

Array of verifiers (id, labels, flow hints, etc.) plus **`meta.count`**. Full shape comes from the tool schema on the wire.

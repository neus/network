---
title: Verifier Catalog (`neus_verifiers_catalog`)
description: Live verifier IDs and metadata from NEUS.
icon: table
---

Call this before hardcoding **`verifierIds`** or policy rules so your integration stays aligned with the current catalog.

## Input

```json
{}
```

## Output

An array of verifiers (id, labels, flow hints, and more) plus **`meta.count`**. The full response shape is defined by the tool schema.

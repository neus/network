---
title: "NEUS MCP"
sidebarTitle: "MCP"
description: "The portable trust harness for AI. Private by default. One endpoint. One sign-in. Trust that travels."
icon: plug
"og:image": "/images/og-mcp.png"
"twitter:image": "/images/og-mcp.png"
---

Bring your **identity, proofs, permissions, agents, and trusted context** to any MCP client.

**Private by default. Portable across models, apps, and runtimes.**

**One endpoint. One sign-in. Trust that travels.**

`https://mcp.neus.network/mcp`

```bash
npx -y -p @neus/sdk neus setup
npx -y -p @neus/sdk neus auth
npx -y -p @neus/sdk neus doctor --live
```

## Share proof. Not private data.

Your AI uses the proof it needs. The data stays with you.

Don't restart from zero. Same profile. New agent.

```text
Private profile
      │
      ├── Identity
      ├── Proofs
      ├── Permissions
      ├── Agents
      ├── Context
      └── Secrets
             │
             ▼
          NEUS MCP
             │
     ┌───────┼───────┐
     ▼       ▼       ▼
   Cursor   Codex   Claude
             │
             ▼
       Your runtime
```

Prove once. Verify anywhere.

[Connect NEUS](./setup)

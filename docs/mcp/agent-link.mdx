---
title: Agent readiness (`neus_agent_link`)
description: Check whether an agent wallet has identity and required proofs.
icon: robot
---

`neus_agent_link` checks **`agentWallet`**: identity (and delegation if you require it) is present.

- **`linked: true`** means ready.
- **`link_required`** means open **`hostedVerifyUrl`** or follow **`nextSteps`**.

Optional **`principal`** hints the **approver** account (not the agent wallet). [Agent concepts](../agents/concepts)

## Input

```json
{ "agentWallet": "0x…" }
```

## Related

[Agent create](./agent-create)

---
title: Context (`neus_context`)
description: Live verifier catalog, constraints, billing hints.
icon: database
---

Call **`neus_context`** first on each **new MCP connection**. **No access key.**

| Provides | Use before |
|------------|------------|
| **`requiredFields`**, chain hints, live verifier constraints | **`neus_proofs_check`**, **`neus_verify`**, **`neus_verifiers_catalog`** (avoids stale verifier IDs) |

Playbook: [Overview](./overview).

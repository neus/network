---
title: Widgets Overview
description: "React widgets — VerifyGate and ProofBadge."
icon: puzzle-piece
"og:image": "/images/og-widgets.png"
"twitter:image": "/images/og-widgets.png"
---

React widgets gate UI behind verification: **`VerifyGate`** for reuse-first gating and **`ProofBadge`** for status. The default path is a published gate — pass one **`gateId`**.

## Install

```bash
npm install @neus/sdk react react-dom
```

| Widget | For |
|--------|-----|
| VerifyGate | Gated UI |
| ProofBadge | Status badge |

## VerifyGate

Create mode opens **Hosted Verify**. The published gate owns verifier inputs, pricing, and checkout policy. [Security and trust](../platform/security-and-trust)

```jsx
import { VerifyGate } from '@neus/sdk/widgets';

export default function ProtectedPage() {
  return (
    <VerifyGate gateId="gate_your-app-name">
      <ProtectedContent />
    </VerifyGate>
  );
}
```

## ProofBadge

```jsx
import { ProofBadge } from '@neus/sdk/widgets';

<ProofBadge qHash={proof.qHash} showChains />
```

## Next

<CardGroup cols={3}>
  <Card title="VerifyGate" icon="object-group" href="./verifygate">
  </Card>
  <Card title="Integration" icon="compass" href="../integration">
  </Card>
</CardGroup>

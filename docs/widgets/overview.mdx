---
title: Widgets Overview
description: React components for gating and proof display.
icon: puzzle-piece
---

**`appId`** / **`sponsorGrant`** same as SDK. **[Get started](../get-started)**

## Install

```bash
npm install @neus/sdk react react-dom
```

| Widget | For |
|--------|-----|
| `VerifyGate` | Gated UI |
| `ProofBadge` | Receipt display |

## VerifyGate

```jsx
import { VerifyGate } from '@neus/sdk/widgets';

export default function ProtectedPage() {
  return (
    <VerifyGate
      requiredVerifiers={['nft-ownership']}
      verifierData={{
        'nft-ownership': {
          contractAddress: '0x...',
          tokenId: '1',
          chainId: 1,
        },
      }}
    >
      <ProtectedContent />
    </VerifyGate>
  );
}
```

## ProofBadge

```jsx
import { ProofBadge } from '@neus/sdk/widgets';

<ProofBadge proofId="0x..." showChains />
```

## Next

<CardGroup cols={2}>
  <Card title="VerifyGate" icon="object-group" href="./verifygate">
    Props + examples.
  </Card>
  <Card title="Quickstart" icon="rocket" href="../quickstart">
    First proof.
  </Card>
</CardGroup>

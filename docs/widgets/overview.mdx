---
title: Widgets Overview
description: "React widgets — VerifyGate and ProofBadge."
icon: puzzle-piece
"og:image": "/images/og-widgets.png"
"twitter:image": "/images/og-widgets.png"
---

The fastest way to add trust to a React app: **`VerifyGate`** gates content behind a check, and **`ProofBadge`** shows verified status. Your app uses the same checks your server already trusts — no verifier logic in the browser.

- **`VerifyGate`** reuses an existing receipt, opens Hosted Verify only when needed, then renders your content. It runs on a published gate — pass one **`gateId`**.
- **`ProofBadge`** displays status for any receipt — pass a **`qHash`**, no gate required.

## Install

```bash
npm install @neus/sdk react react-dom
```

| Export | For |
|--------|-----|
| `VerifyGate` | Gate content behind a check |
| `ProofBadge` | Status badge with chains popover |
| `SimpleProofBadge` | Minimal verified badge |
| `NeusPillLink` | "Verified on NEUS" pill linking to the receipt |
| `VerifiedIcon` | Standalone verified icon |

All are imported from `@neus/sdk/widgets`.

## VerifyGate

Create mode opens **Hosted Verify**. The published gate owns verifier inputs, pricing, and checkout policy. [Security and trust](../platform/security-and-trust)

```jsx
import { VerifyGate } from '@neus/sdk/widgets';

export default function ProtectedPage() {
  return (
    <VerifyGate gateId="gate_your-app-name">
      <ProtectedContent />
    </VerifyGate>
  );
}
```

## ProofBadge

```jsx
import { ProofBadge } from '@neus/sdk/widgets';

<ProofBadge qHash={proof.qHash} showChains />
```

`ProofBadge` also accepts `showLabel`, `logoUrl`, `onClick`, and `className`. See the [VerifyGate](./verifygate) page for the full `VerifyGate` prop list (`requiredVerifiers`, `onVerified`, `strategy`, `checkExisting`, and more).

## Next

<CardGroup cols={3}>
  <Card title="VerifyGate" icon="object-group" href="./verifygate">
  </Card>
  <Card title="Integration" icon="compass" href="../integration">
  </Card>
</CardGroup>

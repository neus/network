---
title: Widgets Overview
description: React components for gating and proof display.
icon: puzzle-piece
---

**`VerifyGate`** is the reuse-first builder surface for React. **`appId`** and **`sponsorGrant`** match the SDK. Complete **[Get started](../get-started)** before production.

## Install

```bash
npm install @neus/sdk react react-dom
```

| Widget | For |
|--------|-----|
| `VerifyGate` | Gated UI |
| `ProofBadge` | Receipt display |

## VerifyGate

Create mode defaults to **unlisted public** (`privacyLevel: 'public'`, `publicDisplay: false`, `storeOriginalContent: true`). Override **`proofOptions`** for listed public or private. Matrix: **[Security and trust](../platform/security-and-trust)**.

```jsx
import { VerifyGate } from '@neus/sdk/widgets';

export default function ProtectedPage() {
  return (
    <VerifyGate
      appId="your-app-id"
      requiredVerifiers={['nft-ownership']}
      verifierData={{
        'nft-ownership': {
          contractAddress: '0x...',
          tokenId: '1',
          chainId: 1,
        },
      }}
    >
      <ProtectedContent />
    </VerifyGate>
  );
}
```

## ProofBadge

```jsx
import { ProofBadge } from '@neus/sdk/widgets';

<ProofBadge proofId="0x..." showChains />
```

## Next

<CardGroup cols={2}>
  <Card title="VerifyGate" icon="object-group" href="./verifygate">
    Props + examples.
  </Card>
  <Card title="Integration" icon="compass" href="../integration">
    Check, verify, save, reuse.
  </Card>
</CardGroup>

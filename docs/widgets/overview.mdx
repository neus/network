---
title: Widgets Overview
description: React: VerifyGate and ProofBadge.
icon: puzzle-piece
---

**VerifyGate** runs reuse-first gating. Pass the same **`appId`** as the SDK. [Get started](../get-started).

## Install

```bash
npm install @neus/sdk react react-dom
```

| Widget | For |
|--------|-----|
| VerifyGate | Gated UI |
| ProofBadge | Status badge |

## VerifyGate

Defaults: **private** (`privacyLevel: 'private'`, `publicDisplay: false`). Override `proofOptions` only when you intentionally need public visibility. [Security and trust](../platform/security-and-trust)

```jsx
import { VerifyGate } from '@neus/sdk/widgets';

export default function ProtectedPage() {
  return (
    <VerifyGate
      appId="your-app-id"
      requiredVerifiers={['nft-ownership']}
      verifierData={{
        'nft-ownership': {
          contractAddress: '0x...',
          tokenId: '1',
          chainId: 1,
        },
      }}
    >
      <ProtectedContent />
    </VerifyGate>
  );
}
```

## ProofBadge

```jsx
import { ProofBadge } from '@neus/sdk/widgets';

<ProofBadge proofId="0x..." showChains />
```

## Next

<CardGroup cols={3}>
  <Card title="VerifyGate" icon="object-group" href="./verifygate">
  </Card>
  <Card title="Integration" icon="compass" href="../integration">
  </Card>
  <Card title="Billing" icon="credit-card" href="../platform/billing">
  </Card>
</CardGroup>

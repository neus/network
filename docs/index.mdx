---
title: "Portable Trust Infrastructure"
sidebarTitle: "Home"
description: "Don't restart from zero. NEUS is portable trust infrastructure for people and AI, one private profile for identity, context, permissions, and proof that works across apps, agents, services, and transactions."
icon: house
"og:image": "/images/og-default.png"
"twitter:image": "/images/og-default.png"
---

Don't restart from zero.

Every new app makes you reconnect yourself. Re-prove ownership. Reconfigure permissions. Reattach tools. NEUS collapses that cost. One private profile for your identity, context, permissions, and proof. Portable across apps, agents, services, and transactions. Built for people and AI.

<CardGroup cols={3}>
  <Card title="Profile" icon="user">
    What should persist. Identity, context, permissions, and proof in one private place.
  </Card>
  <Card title="Proof" icon="brackets-curly">
    What travels. A reusable artifact any connected system can check.
  </Card>
  <Card title="Gate" icon="shield-check">
    What is allowed. Enforce authority before access, action, or payment.
  </Card>
</CardGroup>

## Your AI shouldn't start from zero either

Give any agent the identity, context, tools, permissions, and proofs it needs without rebuilding everything for every model or framework. Same profile. New agent. Your AI has access. NEUS gives it authority.

## Connect in one command

<Tabs sync={false}>
  <Tab title="All editors">

```bash
npx -y -p @neus/sdk neus setup
```

  </Tab>
  <Tab title="Cursor">

```bash
npx -y -p @neus/sdk neus setup --client cursor
```

  </Tab>
  <Tab title="Claude Code">

```bash
npx -y -p @neus/sdk neus setup --client claude
```

  </Tab>
  <Tab title="Codex">

```bash
npx -y -p @neus/sdk neus setup --client codex
```

  </Tab>
  <Tab title="VS Code">

```bash
npx -y -p @neus/sdk neus setup --client vscode
```

  </Tab>
</Tabs>

Confirm the connection is live:

```bash
npx -y -p @neus/sdk neus doctor --live
```

One setup. Same identity, provenance, and limits in Cursor, Claude Code, Codex, and production.

## Or paste this into your agent

Copy the block below into Claude Code, Cursor, Codex, or any coding agent with NEUS MCP. The agent reads it, runs setup, signs you in, and starts carrying your authority.

```text
# NEUS connect in one prompt

You are connecting this editor to NEUS so my identity, provenance, and
authority travel with me. Follow these steps exactly.

## 1. Register NEUS
Run: npx -y -p @neus/sdk neus setup
This installs the public workflow skill and writes MCP config for every
supported host in this project. It does not open a browser.

## 2. Sign in
Run: npx -y -p @neus/sdk neus auth
Or click Connect in the MCP client. Default sign-in is browser OAuth.
No wallet required.

## 3. Confirm the connection
Run: npx -y -p @neus/sdk neus doctor --live
A healthy result confirms the endpoint, auth, and public tools.

## 4. What to do next
After setup, reuse my existing proofs before asking me to verify again.
Before any sensitive action (spend, publish, secrets, agent tool call),
call neus_context once, then neus_proofs_check. Summarize as
NEUS Verify: Passed, Action needed, or Blocked.

Canonical endpoint: https://mcp.neus.network/mcp
Docs: https://docs.neus.network/mcp/setup
```

## What you can build

<CardGroup cols={3}>
  <Card title="Control an agent" icon="robot" href="./agents/agent-verification-flow">
    Identity, spend caps, revocation. Enforced before action.
  </Card>
  <Card title="Gate and monetize" icon="rocket" href="./quickstart">
    Gate once. Monetize every qualified visitor.
  </Card>
  <Card title="Ship with the SDK" icon="code" href="./sdks/overview">
    Hosted check, server confirm, React gate.
  </Card>
</CardGroup>

<CardGroup cols={3}>
  <Card title="Wire MCP tools" icon="plug" href="./mcp/overview">
    One endpoint. Any agent, editor, or server.
  </Card>
  <Card title="Check before action" icon="shield-check" href="./verification/overview">
    Run once, save the proof, reuse everywhere.
  </Card>
  <Card title="Read the API" icon="terminal" href="./api/overview">
    Hosted checks, proofs, authority, billing. OpenAPI.
  </Card>
</CardGroup>

## How it works

Verify once. Carry the proof. Enforce before action.

Frameworks run tools. Models think. NEUS checks identity, provenance, and authority before sensitive actions.

1. **Verify once.** Run a check through the hosted screen, MCP, or SDK.
2. **Carry the proof.** NEUS saves the result with its status, allowed use, and expiration.
3. **Enforce before action.** Connected apps require that proof before access, payment, or execution.

Read the full flow in [How verification works](./verification/how-it-works), [Proof lifecycle](./verification/lifecycle), and [Proof links and versioning](./learn/proof-links).

## Where NEUS sits

Login gets them in. Authority decides what they do next. NEUS records that authority as a portable proof any connected app can check, so trust travels with the actor, not the app.

- Keep your sign-in, identity provider, payment, and agent framework.
- Add NEUS via SDK, API, or MCP. No replacement required.
- No wallet required. Work accounts, domains, social accounts, and passkeys all work. Wallet checks are optional.

See how teams put this to work in [Solutions](https://neus.network/solutions).

Trust that travels.

## Status

NEUS is under active development. Published APIs and verifier schemas may change between minor versions before the long-term stability freeze. We version everything we ship and document every breaking change in the [changelog](https://github.com/neus/network/blob/main/CHANGELOG.md).

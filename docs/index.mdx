---
title: "Verify once. Carry the proof."
sidebarTitle: "Home"
description: "Check identity, ownership, and permissions once. Reuse the proof before access, payment, or an agent action. Across every app and runtime."
icon: house
"og:image": "/images/og-default.png"
"twitter:image": "/images/og-default.png"
---

## Connect NEUS in one command

<Tabs sync={false}>
  <Tab title="All editors">

```bash
npx -y -p @neus/sdk neus setup
```

  </Tab>
  <Tab title="Cursor">

```bash
npx -y -p @neus/sdk neus setup --client cursor
```

  </Tab>
  <Tab title="Claude Code">

```bash
npx -y -p @neus/sdk neus setup --client claude
```

  </Tab>
  <Tab title="Codex">

```bash
npx -y -p @neus/sdk neus setup --client codex
```

  </Tab>
  <Tab title="VS Code">

```bash
npx -y -p @neus/sdk neus setup --client vscode
```

  </Tab>
</Tabs>

Check the connection:

```bash
npx -y -p @neus/sdk neus doctor --live
```

## Start with the outcome

<CardGroup cols={3}>
  <Card title="Control an agent" icon="robot" href="./agents/agent-verification-flow">
    Set identity, spend limits, and what each agent may do. Revoke anytime.
  </Card>
  <Card title="Gate and monetize" icon="puzzle-piece" href="./quickstart">
    Require a current check before someone sees content, pays, or runs a tool.
  </Card>
  <Card title="Build with the SDK" icon="code" href="./sdks/overview">
    Add a hosted check, confirm on your server, and embed a gate in React.
  </Card>
</CardGroup>

See how teams put this to work: [Solutions](https://neus.network/solutions).

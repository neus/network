---
title: Verification Overview
description: The NEUS verification contract from request and signing through proof receipt reuse.
icon: file-signature
---

## Default Path

1. **Choose the verifier you need**
2. **Launch hosted verify or use the SDK**
3. **Receive a proof receipt ID**
4. **Reuse that receipt for access, display, and policy**

## Key Terms

| Term | Meaning |
|------|---------|
| Request | The verifier set plus the normalized proof payload |
| Signing string | The deterministic wallet message returned by `POST /api/v1/verification/standardize` |
| Proof receipt ID | The stable identifier returned for a proof |
| `qHash` | The HTTP/path name for the same proof receipt ID in raw API contracts |

## Where To Go Next

<CardGroup cols={2}>
  <Card title="How Verification Works" icon="shield-check" href="./how-it-works">
    Understand request flow, signing, and hosted interaction.
  </Card>
  <Card title="Lifecycle" icon="rotate" href="./lifecycle">
    See async processing, terminal states, reuse, and revocation.
  </Card>
  <Card title="Signing Format" icon="file-code" href="./signing-format">
    Read the standardized signing format and production rules.
  </Card>
  <Card title="Verifier Catalog" icon="list-tree" href="./verifiers">
    Pick the verifier that matches the real-world claim you need.
  </Card>
</CardGroup>

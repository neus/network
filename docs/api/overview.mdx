---
title: API Overview
description: REST endpoints for proof checks, reads, and advanced HTTP verification.
icon: terminal
---

Use the HTTP API when a server or tool needs **eligibility checks**, **proof reads**, or **verification without the JavaScript SDK**. Responses use the same proof ids as the SDK; usage bills to the NEUS profile linked to your app.

**Base URL:** `https://api.neus.network`

### Browsers

Treat this API as **server-to-server** (your backend, workers, CLI, MCP). In end-user browsers, use the **[JavaScript SDK](../sdks/javascript)** or **[Hosted Verify](../cookbook/auth-hosted-verify)**. If a web UI needs headers or methods outside those flows, **your app’s backend** should call NEUS—not `fetch` from the page—so you avoid CORS mistakes and keep secrets off the client.

### Creating proofs

Use **GET** endpoints below for checks and reads.

## Endpoints

| Path | Method | Purpose |
|------|--------|---------|
| `/api/v1/proofs/check` | GET | Eligibility (server gates) |
| `/api/v1/proofs/by-wallet/{address}` | GET | List by wallet |
| `/api/v1/proofs/{qHash}` | GET | Fetch by id (`qHash` same as SDK `proofId`) |
| `/api/v1/verification` | POST | Create proof (**advanced** - see [Authentication](./authentication)) |
| `/api/v1/verification/standardize` | POST | **Mandatory phase 1** for raw HTTP: exact `signerString` for the body you will submit |
| `/api/v1/verification/verifiers` | GET | Catalog |

## Examples

**By wallet**

```bash
curl "https://api.neus.network/api/v1/proofs/by-wallet/0x...?limit=50"
```

Public / unlisted: anonymous ok. **Private:** requires access as the proof owner ([Authentication](./authentication)).

**Check**

```bash
curl "https://api.neus.network/api/v1/proofs/check?address=0x...&verifierIds=ownership-basic"
```

**Create a proof**

For most apps, create proofs via the SDK or the hosted browser flow at `https://neus.network/verify` (**Hosted Verify**). Raw HTTP is a **strict two-phase handshake**, not a single-step request:

1. Build the exact JSON body you will submit.
2. **`POST /api/v1/verification/standardize`** with that body.
3. Sign the returned **`signerString`**.
4. **`POST /api/v1/verification`** with the **same JSON body** plus `signature`.

If phase 2 fails with a signature error, re-run phase 1 on the same payload and compare the signer string. Shapes: **API reference** on the documentation site.

**Catalog**

```bash
curl https://api.neus.network/api/v1/verification/verifiers
```

## Auth

- **Reads:** public and unlisted proofs without a caller; private proofs only with allowed access.
- **Writes:** wallet signature, or authenticated caller when the API accepts it—[Authentication](./authentication)

## Next

<CardGroup cols={2}>
 <Card title="Get started" icon="book" href="../get-started">
 Onboarding.
 </Card>
 <Card title="Authentication" icon="key" href="./authentication">
 Signing.
 </Card>
 <Card title="Errors" icon="triangle-exclamation" href="./errors">
 Codes.
 </Card>
</CardGroup>

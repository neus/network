# NEUS Trust MCP Metadata

NEUS Trust MCP discovery metadata lives at:

**[`../../mcp/npm-dist/server.json`](../../mcp/npm-dist/server.json)**

It includes:

- Server identity (`io.github.neus/neus-mcp`)
- Remote HTTP endpoint (`https://mcp.neus.network/mcp`)
- OAuth protected-resource metadata URL
- Tool surface catalog
- Compatibility matrix

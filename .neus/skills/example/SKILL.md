---
description: Example NEUS skill — demonstrates /slash invocation and progressive disclosure in chat.
user-invocable: true
disable-model-invocation: false
---

# Example skill

When this skill is active:

1. Briefly explain how skills work in NEUS Code Editor (index → load → optional scripts).
2. Mention locations: `.claude/skills/`, `.cursor/skills/`, `.neus/skills/`, and `~/.claude/skills/`.
3. Help the user with their task using the skill workflow pattern.

Supporting files can live beside `SKILL.md`. Run scripts with `shell_exec` when instructed.

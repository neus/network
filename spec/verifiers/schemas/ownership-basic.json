{
  "$id": "https://neus.network/schemas/ownership-basic.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ownership-basic",
  "description": "Content ownership verification with origin lock and optional provenance",
  "type": "object",
  "required": [
    "owner"
  ],
  "properties": {
    "content": {
      "type": "string",
      "maxLength": 50000,
      "description": "Inline text proved at verify time (max 50KB). Send in verify request data.content. When storeOriginalContent is enabled, persisted body is exposed on proof.publicContent.content."
    },
    "contentHash": {
      "type": "string",
      "pattern": "^0x[a-fA-F0-9]{64}$",
      "description": "Ownership-basic verifier hash only: 0x + 64 hex. Not an arbitrary SHA256 file digest. Omit if unknown; submit content instead and use verifierContentHash from the receipt. When both content and contentHash are sent, they must match."
    },
    "contentType": {
      "type": "string",
      "maxLength": 100,
      "description": "MIME type hint"
    },
    "owner": {
      "type": "string",
      "description": "Owner identifier (must match request walletAddress). Supports EVM (0x..), Solana (base58), NEAR (account IDs), and other universal address formats depending on chain context.",
      "format": "universal-address",
      "minLength": 1
    },
    "reference": {
      "type": "object",
      "required": [
        "type"
      ],
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "ipfs",
            "ipfs-hash",
            "url",
            "license-nft",
            "contract",
            "qhash",
            "ethereum-tx",
            "on-chain-tx",
            "tx",
            "file",
            "doc",
            "media",
            "username-claim",
            "other"
          ],
          "description": "Reference type"
        },
        "id": {
          "type": "string",
          "maxLength": 2048,
          "description": "Reference identifier"
        },
        "title": {
          "type": "string",
          "maxLength": 200
        },
        "description": {
          "type": "string",
          "maxLength": 500
        },
        "mime": {
          "type": "string",
          "maxLength": 100
        },
        "name": {
          "type": "string",
          "maxLength": 255
        },
        "size": {
          "type": "integer",
          "minimum": 0
        }
      }
    },
    "provenance": {
      "type": "object",
      "properties": {
        "declaredKind": {
          "type": "string",
          "enum": [
            "human",
            "ai",
            "mixed",
            "unknown"
          ]
        },
        "aiContext": {
          "type": "object",
          "properties": {
            "generatorType": {
              "type": "string",
              "enum": [
                "local",
                "saas",
                "agent"
              ]
            },
            "provider": {
              "type": "string",
              "maxLength": 64
            },
            "model": {
              "type": "string",
              "maxLength": 128
            },
            "runId": {
              "type": "string",
              "maxLength": 128
            }
          }
        }
      }
    }
  }
}

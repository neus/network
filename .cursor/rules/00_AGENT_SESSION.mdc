---
description: Public SDK and spec — align changes with live API and network/spec.
alwaysApply: true
---

# Agent-driven session (NEUS network)

Before changing **SDK surface**, **public spec**, or **verifier schemas**:

1. Align with **`GET /api/v1/verification/verifiers`**, **`docs/openapi/public-api.json`**, and **`spec/`**—no invented fields or verifier IDs.
2. Run **`npm test`** / lint as in **`package.json`** before claiming done.

Repo index: **`AGENTS.md`** in this folder.

---
title: SDK Overview
description: Start with the NEUS JavaScript SDK for proof creation, hosted handoff, polling, and private proof access.
---

# SDK Overview

The JavaScript SDK is the primary client surface for custom browser and app integrations. It wraps verification creation, deterministic signing helpers, hosted verify handoff, proof polling, and widget exports in one package.

## What the SDK covers

<CardGroup cols={2}>
  <Card title="Create and poll proofs" icon="code" href="./javascript">
    Submit verification requests, standardize signing, and poll async proof status.
  </Card>

  <Card title="Authentication helpers" icon="key" href="./authentication">
    Use hosted login and auth code exchange guidance for lower-friction browser flows.
  </Card>

  <Card title="Verification patterns" icon="shield" href="./verifications">
    Choose reuse, fresh proof, or gate-check strategies based on policy and UX needs.
  </Card>

  <Card title="Error handling" icon="circle-exclamation" href="./error-handling">
    Handle retries, hosted flow failures, interactive verifier handoff, and private proof auth.
  </Card>
</CardGroup>

## Install

```bash
npm install @neus/sdk
```

## Typical flow

```javascript
import { NeusClient } from '@neus/sdk';

const client = new NeusClient({
  apiUrl: 'https://api.neus.network',
  appId: 'acme-web'
});
```

From there, move into:

1. [JavaScript SDK](./javascript) for API shape and examples
2. [Verification patterns](./verifications) for proof lifecycle choices
3. [Widgets](../widgets/overview) if you want drop-in UI instead of custom UX
